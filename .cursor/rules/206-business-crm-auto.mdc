---
description: CRM — regras de negócio e integração BFF (confirmado)
globs:
  - crm/**/*
  - setup/settings.py
  - setup/urls.py
  - base_templates/global/partials/_header.html
alwaysApply: false
---

# CRM — regras de negócio (confirmado)

## Arquitetura

- **BFF Django** sobre API FastAPI CRM (`CRM_API_BASE_URL` + `CRM_API_V1_STR` em `setup/local_settings.py`).
- **Zero** migrations/tabelas `crm_*` no Django — permissões `app_label=crm` vêm do seed Alembic no banco compartilhado.
- Browser **nunca** chama a API CRM diretamente; 100% via `CrmApiClient` em `crm/services/client.py`.
- `CRM_INTERNAL_API_SECRET` (Bearer) + headers `X-CRM-*` — **nunca** expor em HTML, JS ou logs.

```text
Browser → Django (@crm_perm_required) → CrmApiClient → FastAPI api-crm
```

## Gate de acesso

| Camada | Regra |
|--------|-------|
| Baseline | `login` + `logistica.acesso_arancia` (`crm_access_required`) |
| Menu/dashboard | ao menos uma `crm.view_*` (`crm_module_required`) |
| Módulo | permissão específica (`crm_perm_required`, ex. `crm.view_tasks`) |
| GAI | `UserDesignation.informacao_adicional_id` obrigatório — sem GAI, mensagem amigável sem chamar API |

Headers: `X-CRM-Designation-IDs` = **um** ID (OneToOne legado).

## Escopo e elevação

- Usuário comum: API filtra pelo contexto `X-CRM-*`; Django não amplia escopo.
- Menu de boards: `GET /me/context` → `accessible_boards` (cache sessão 300s).
- Kanban: UI habilitada por `GET /boards/{id}/access/me` (`can_move_tasks`, `can_comment`).
- Detalhe de task com board: comentários/anexos exigem `can_comment` do board **e** `crm.change_task`.
- Self-watch (`POST /tasks/{id}/watch`) funciona sem `crm.manage_watchers`; observadores de terceiros exigem `manage_watchers`.
- Jobs Celery: `CrmApiClient(service=True)` + `build_service_crm_headers()`.

## Rotas BFF implementadas (colaboração)

| Área | Rotas Django | API proxy |
|------|--------------|-----------|
| Task links | `ajax/tasks/<id>/links/` | `POST/DELETE /tasks/{id}/links` |
| Assignees | `ajax/tasks/<id>/assignees/<id>/` | `PATCH/DELETE` |
| Watchers | `ajax/tasks/<id>/watchers/` | `POST/DELETE /tasks/{id}/watchers` |
| Move history | `ajax/tasks/<id>/move-history/` | `GET /tasks/{id}/move-history` |
| Boards CRUD | `crm/boards/`, `new/`, `<id>/edit/` | `GET/POST/PATCH/DELETE /boards/` |
| Colunas | `crm/boards/<id>/settings/` | `GET/POST/PATCH /boards/{id}/columns` |
| Acesso board | inline + `ajax/.../access/<id>/` | `PATCH /boards/{id}/access/{id}` |
| Recorrências | `crm/tasks/recurrences/` | `GET/POST/PATCH/DELETE /task-recurrences/` |
| Projeto tasks | `crm/projects/<id>/tasks/` | `GET /tasks/?project_id=` |
| Lookups AJAX | `crm/ajax/lookups/crm/` | `GET /lookups/crm` |
| Lookups grupos/GAIs | `crm/ajax/lookups/groups/`, `crm/ajax/lookups/gais/` | `GET /lookups/groups`, `GET /lookups/gais` |
| Reorder status | `ajax/settings/status-tasks/reorder/` | `PATCH /status-tasks/reorder` |

## Módulos e gaps API (não implementar no BFF)

| Recurso | Motivo |
|---------|--------|
| DELETE contrato / faturamento | Sem endpoint |
| PATCH/DELETE contato/endereço de cliente | Sem endpoint |
| DELETE anexo de task | Sem endpoint |
| CRUD `/teams/*` | Removido — usar `GET /lookups/team-gais` |
| Paginação com total count | Sem endpoint |
| Dashboard dedicado na API | Compor com `/billing/summary`, `/alerts/`, `/tasks/my/` |

Fechado no BFF (2026-06): task links (`target_task_id`), watchers, assignee patch/delete, boards CRUD/colunas, recorrências UI + scheduler one-shot, reorder status-tasks, demandantes GAI, gates centralizados.

## Cadastro de cliente CRM (`clients/new/`)

- Vincula GAI existente via **`PATCH /clients/{gai_id}`** (não usar `POST /clients/` — schema sem `gai_id`).
- Dropdown **GAI / Cliente**: GAIs do grupo Django **`arancia_client`** (`GET /lookups/gais?group_id=…`) **excluindo** os já em `GET /lookups/crm` → `customers`.
- **Não** reutilizar `/lookups/gais` sem filtro — esse endpoint é para **demandantes de tarefa** (PA, CUSTOMER, TI, etc.).
- `arancia_CUSTOMER` (transportes/localidades) ≠ `arancia_client` (clientes comerciais CRM).

## Demandantes GAI (tarefas de projeto)

- Campo **`requester_gai_ids[]`** no create/edit de task quando há `project_id` (formulário unificado em `crm/tasks/new/`).
- Lookups: `GET /lookups/groups` (filtro) e `GET /lookups/gais` (`group_id`, `search` opcionais).
- AJAX BFF: `crm/ajax/lookups/groups/`, `crm/ajax/lookups/gais/` — browser nunca chama API diretamente.
- Detalhe exibe `task.requesters[]` (EntityRef: `r.gai.nome`, `r.gai_id`, etc.).
- Recorrência: incluir `requester_gai_ids` no payload de `POST /task-recurrences/` quando aplicável.

## Gates GAI

- Helpers em `crm/services/gates.py`: `require_gai_or_render`, `ajax_require_gai`.
- Views CRM importam wrapper local com `site_title` e menu; **não** duplicar lógica de checagem GAI inline.

## EntityRef em templates

Helpers em `crm/services/refs.py` (`customer_ref_label`, `contract_ref_label`, `subject_ref_label`, etc.) e filtros em `crm/templatetags/crm_tags.py` (`crm_customer_label`, `crm_contract_label`, …).

- **Exibição:** preferir objeto aninhado da API → fallback flat legado (`status_name`, `board_name`) → fallback `*_id` → lookup Django só quando ref ausente.
- **Formulários POST/PATCH:** enviar apenas `*_id` no payload — nunca montar labels no POST.
- **Lookups AJAX:** usar para choices de forms, não para labels de listagem quando o ref já veio na response.
- Task links: POST à API com **`target_task_id`** (não `linked_task_id`).

## Checklist homologação (§12)

- Permissões `crm.view_*` / `crm.add_*` por módulo via decorators.
- 403 de escopo → mensagem PT-BR (`handle_crm_error`), não 500.
- Token/secret nunca em HTML, JS ou logs.
- 100% chamadas HTTP CRM via `CrmApiClient`.
- Upload contrato/anexo via `post_multipart`.
- Kanban move persiste `status_id` + `kanban_position`; UI por `GET /boards/{id}/access/me`.
- Celery alertas + scheduler idempotentes (skip se CRM não configurado).
- Form unificado: avulsa → card imediato; recorrente vencida → scheduler one-shot → primeira instância.

## Variáveis de ambiente

Ver `.env.example`: `CRM_API_BASE_URL`, `CRM_API_V1_STR`, `CRM_INTERNAL_API_SECRET`, `CRM_API_TIMEOUT`, `CRM_API_VERIFY_SSL`, `CRM_DEFAULT_LIMIT`, `CRM_ENABLE_DEBUG_LOGS`, `CRM_SERVICE_USER_ID`, `CRM_SERVICE_USERNAME`.

## Celery

| Task | Endpoint | Horário beat |
|------|----------|--------------|
| `crm_fire_due_alerts` | `POST /alerts/fire/{id}` (lista + filtro local) | 06:00 |
| `crm_generate_recurring_tasks` | `POST /internal/scheduler/generate-due-tasks` | 06:30 |

## Ao criar ou alterar telas CRM

- Reutilizar padrão de `view_clients.py`: GAI gate, lookups, paginação skip/limit, `handle_crm_error`.
- Datetimes de tarefas: `crm/services/datetime_utils.py` (BR ↔ UTC).
- Upload multipart: `post_multipart` no BFF (contratos, anexos de task).
- Membros de projeto e acesso a board: **exatamente um** de `user_id`, `designation_id`, `team_gai_id`.
- Demandantes de task de projeto: lista `requester_gai_ids[]` (zero ou mais GAIs elegíveis no escopo do usuário).

## Proibido

- Criar `crm/models.py` com tabelas de negócio CRM.
- Retornar dados de todos os clientes sem elevação explícita na API.
- Hardcodar URL/token da API em templates ou JavaScript.
- Unificar mecanismos de escopo GAI com outras flags do projeto em refatorações não solicitadas.
